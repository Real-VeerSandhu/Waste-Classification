import tensorflow as tf
import numpy as np

# config = tf.compat.v1.ConfigProto()
# config.gpu_options.allow_growth = True
# sess = tf.compat.v1.Session(config=config)

# loaded_model = keras.models.load_model('/models/resnet50_gar0.h5')


